import Dashboard from './components/Dashboard.jsx';

/**
 * Application root — renders the insights dashboard. Global providers (React Query,
 * Router) are set up in main.jsx.
 */
function App() {
  return <Dashboard />;
}

export default App;
